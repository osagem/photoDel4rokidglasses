<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
  Android 13 (API 33) 及以上版本推荐使用 granular media permissions.
  如果您的应用仅需要访问自己创建的媒体文件或需要照片选择器，则不需要这些权限。
  但由于您需要浏览特定目录（Camera, Pictures），这些是必须的。
-->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!--
      为了在 Android 11 (API 30) 及以上版本获得管理所有文件的能力，请使用此权限。
      这是特殊应用访问权限，需要引导用户到系统设置页面手动授予。
      tools:ignore="ScopedStorage" 用于告诉 Lint 检查器，我们知道这个权限会绕过分区存储。
    -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!--
      为了兼容 Android 10 (API 29) 及以下版本，仍然需要 WRITE_EXTERNAL_STORAGE。
      maxSdkVersion="29" 确保此权限仅在 Android 11 之前的设备上请求。
    -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!--
      为了兼容 Android 12 (API 32) 及以下版本，读取权限是必要的。
      在更高版本上，它作为 READ_MEDIA_* 的回退。建议保留，但限制到32。
    -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <queries>
        <package android:name="com.android.camera2" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/applogo"
        android:label="@string/app_name"
        android.roundIcon="@drawable/applogo"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.PhotoView4rokidglasses">
        <activity
            android:name=".PhotoListActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>